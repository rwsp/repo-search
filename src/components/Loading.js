import React from 'react';
import LoadingBar from '../loadingBar.svg';

/**
 *
 * Loading - visual component for animated loading indicator
 *
 */
const Loading = () => <img src={LoadingBar} alt="loading-bar"/>;

export default Loading;